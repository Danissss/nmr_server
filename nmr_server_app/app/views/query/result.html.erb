<p>
	Example Run:
</p>

<div id="sample_graph" width="400" height="400"></div>

<!-- <div class="container" id="graph"></div> -->


<script>
	// var graph = new Graph("sample_graph");

	var data = {
      x: <%= @x_axis %>,
      y: <%= @y_axis %>
    };


    let wave = Graph.newWaveform().setData( data.y, data.x );
    let data2 = JSON.parse( JSON.stringify( data ) );
    let wave2 = Graph.newWaveform().setData( data2.y, data2.x );

    var graph = new Graph('sample_graph', {
      plugins: {
        peakPicking: {}
      }
    });

    graph.resize(1000,1000);
    const eiSerie = graph
      .newSerie( 'ei', { lineToZero: true } )
      .autoAxis()
      .setWaveform( wave );

	graph.getPlugin( 'peakPicking' ).setSerie( eiSerie );

	graph.draw();

</script>


<!-- 

<script>
	


	var ctx = document.getElementById("nmr_graph");
	var datapoint = [{
        x: 10,
        y: 20
    }, {
        x: 15,
        y: 10
    }]

	var myLineChart = new Chart(ctx, {
    	type: 'line',
    	data: datapoint,
    	// data: {
    	// 	labels: "NMR spectrum",
    	// 	datasets:{
    	// 		label: "NMR spectrum",
    	// 		data: datapoint,
    	// 	}
    	// }


    	options:{
    		tooltips: {
    			mode: 'nearest',
    		},

    	// 	animation:{
    	// 		duration: 0,
    	// 	},

    	// 	hover: {
    	// 		animationDuration: 0,
    	// 	},

    	// 	responsiveAnimationDuration: 0,

    		elements:{
    			line:{
    				tension: 0,
    			}
    		},

    		scales: {
    			xAxes: [{
    				type: 'linear',
    				position: 'buttom'
    			}]
    		}
    	}
	});
	Chart.defaults.showLines = true;

</script> -->